[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[[cronjobs]]
name = "daily-reminders"
schedule = "0 13 * * *"
command = "curl -s -X POST https://venyou.subculture.audio/api/cron/daily-reminders -H 'Authorization: Bearer $CRON_SECRET'"
